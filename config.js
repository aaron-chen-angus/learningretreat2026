/* =====================================================================
   Learning Retreat 2026 — deck configuration
   ---------------------------------------------------------------------
   Paste your Google Apps Script Web App URL between the quotes below.
   It must end in /exec. Leave it empty ("") to run in demo mode, where
   votes are only logged to the browser console and nothing is saved.
   ===================================================================== */
window.LR_CONFIG = {
  webhookUrl: ""
};
